<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <!-- Chat bubble -->
    <LinearLayout
        android:id="@+id/layoutSelfMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/shape_sending_message"
        android:orientation="vertical"
        app:layout_constraintEnd_toStartOf="@id/ivUserRight"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/tvMessageFrom"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:layout_marginStart="@dimen/_12sdp"
            android:layout_marginTop="@dimen/_8sdp"
            android:layout_marginEnd="@dimen/_20sdp"
            android:layout_marginBottom="@dimen/_8sdp"
            android:fontFamily="@font/poppins_regular"
            android:lineSpacingMultiplier="1.3"
            android:maxWidth="240dp"
            android:textColor="@color/black"
            android:textSize="@dimen/_12sdp"
            tools:text="This is my own message bubble on the right side." />

        <TextView
            android:id="@+id/tvMessageFromTimestamp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginStart="12dp"
            android:layout_marginTop="2dp"
            android:layout_marginEnd="30dp"
            android:layout_marginBottom="6dp"
            android:fontFamily="@font/poppins_regular"
            android:text="10:55 AM"
            android:textColor="@android:color/darker_gray"
            android:textSize="10sp"
            android:visibility="visible" />
    </LinearLayout>

    <!-- Receiver's profile image (right side) -->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/ivUserRight"
        android:layout_width="@dimen/_40sdp"
        android:layout_height="@dimen/_40sdp"
        android:layout_marginEnd="@dimen/_8sdp"
        android:src="@drawable/ic_default_profile"
        app:civ_border_overlay="true"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
